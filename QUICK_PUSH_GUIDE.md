# Quick Push Guide

## Your Repository Setup ✅

You now have **two remotes** configured:

1. **origin** → `Umit-Azirakhmet/mt-rag-benchmark` (original repo)
2. **myrepo** → `pratirvce/MTRAG-Eval` (your new repo) ✅ **PUSHED SUCCESSFULLY**

## Quick Commands

### Push to Your Repository (Works Now!)
```bash
git push myrepo mtrag_prati_v0
```

### Push to Original Repository (When You Have Access)
```bash
git push origin mtrag_prati_v0
```

### Push to Both Repositories
```bash
git push origin mtrag_prati_v0 && git push myrepo mtrag_prati_v0
```

### View Your Remotes
```bash
git remote -v
```

## Current Status

✅ **Branch `mtrag_prati_v0` successfully pushed to:**
- `pratirvce/MTRAG-Eval` (your repo) - **DONE!**
- `Umit-Azirakhmet/mt-rag-benchmark` (original) - pending access

## View Your Code Online

Your code is now available at:
**https://github.com/pratirvce/MTRAG-Eval/tree/mtrag_prati_v0**

## Future Workflow

1. Make changes locally
2. Commit: `git commit -am "Your message"`
3. Push to your repo: `git push myrepo mtrag_prati_v0`
4. Push to original (when you have access): `git push origin mtrag_prati_v0`

